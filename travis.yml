language: python
python:
  - "2.7"
services: mongodb
env: BOCK_MONGO_TEST_DB=mongodb://localhost/
# command to install dependencies
install: "pip install -r requirements.txt"
# start mongodb
# command to run tests
script: nosetests
